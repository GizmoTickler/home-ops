---
# yaml-language-server: $schema=https://kubernetes-schema.pages.dev/gateway.envoyproxy.io/securitypolicy_v1alpha1.json
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: SecurityPolicy
metadata:
  name: it-tools-oidc
spec:
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      name: it-tools
  oidc:
    provider:
      issuer: https://id.${SECRET_DOMAIN}
    clientID: it-tools
    clientSecret:
      name: it-tools-oidc-secret
    redirectURL: https://it-tools.${SECRET_DOMAIN}/oauth2/callback
    logoutPath: /oauth2/logout
    defaultTokenTTL: 15m
    defaultRefreshTokenTTL: 1h
    scopes:
      - openid
      - email
      - profile
