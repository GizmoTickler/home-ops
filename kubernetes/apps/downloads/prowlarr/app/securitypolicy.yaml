---
# yaml-language-server: $schema=https://kubernetes-schema.pages.dev/gateway.envoyproxy.io/securitypolicy_v1alpha1.json
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: SecurityPolicy
metadata:
  name: prowlarr-oidc
spec:
  targetRefs:
    - group: gateway.networking.k8s.io
      kind: HTTPRoute
      name: prowlarr
  oidc:
    provider:
      issuer: https://id.${SECRET_DOMAIN}
    clientID: prowlarr
    clientSecret:
      name: prowlarr-oidc-secret
    redirectURL: https://prowlarr.${SECRET_DOMAIN}/oauth2/callback
    logoutPath: /oauth2/logout
    defaultTokenTTL: 15m
    defaultRefreshTokenTTL: 1h
    scopes:
      - openid
      - email
      - profile
