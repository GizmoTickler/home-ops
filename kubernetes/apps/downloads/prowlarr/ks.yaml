---
# yaml-language-server: $schema=https://kubernetes-schema.pages.dev/kustomize.toolkit.fluxcd.io/kustomization_v1.json
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: prowlarr
spec:
  components:
    - ../../../../components/nfs-scaler
    - ../../../../components/kopiur/backup
  dependsOn:
    - name: keda
      namespace: observability
  interval: 1h
  path: ./kubernetes/apps/downloads/prowlarr/app
  postBuild:
    substitute:
      APP: prowlarr
      KOPIUR_CAPACITY: 10Gi
      KOPIUR_STORAGECLASS: scale-nvmeof
      KOPIUR_SNAPSHOTCLASS: scale-snapshot
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  targetNamespace: downloads
  wait: false
