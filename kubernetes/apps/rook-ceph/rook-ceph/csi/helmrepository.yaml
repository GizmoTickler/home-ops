---
# yaml-language-server: $schema=https://kubernetes-schema.pages.dev/source.toolkit.fluxcd.io/helmrepository_v1.json
apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: ceph-csi-operator
spec:
  interval: 1h
  # ceph-csi-drivers is only published to this HTTP Helm repo; it is NOT
  # available as an OCI artifact (unlike the rook-ceph operator/cluster charts).
  url: https://ceph.github.io/ceph-csi-operator
