---
# yaml-language-server: $schema=https://kubernetes-schema.pages.dev/v1/configmap.json
# IMPORTANT: This file is ONLY for flux-local CI testing.
# It is NOT a Flux resource and will NOT be deployed to the cluster.
# flux-local reads this to substitute variables when testing Helm charts.
apiVersion: v1
kind: ConfigMap
metadata:
  name: cluster-config-vars
  namespace: flux-system
data:
  SECRET_DOMAIN: "example.com"
