---
minimum_pre_commit_version: "3.6.0"
default_install_hook_types:
  - pre-commit

repos:
  - repo: local
    hooks:
      - id: secret-pattern-scan
        name: Secret Pattern Scan
        entry: scripts/pre-commit/scan-secrets.sh
        language: system
        pass_filenames: true
